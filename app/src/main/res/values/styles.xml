<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="shapeAppearanceLargeComponent">@style/ShapeAppearanceOverlay</item>
        <item name="android:statusBarColor">#92009D</item>
    </style>

    <style name="Widget.MaterialComponents.Button.IconOnly">
        <item name="iconPadding">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:minWidth">12dp</item>
        <item name="android:minHeight">12dp</item>
        <item name="iconGravity">textStart</item>
        <item name="cornerRadius">20dp</item>
    </style>

    <style name="ShapeAppearanceOverlay" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="cornerSize">25dp</item>
        <item name="cornerFamily">rounded</item>
    </style>

</resources>